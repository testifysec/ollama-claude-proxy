# Environment variables for ollama-claude-proxy
# Source this file before running the application
# cp .env.example .env && edit .env to include your API key

# Anthropic API key (required)
export ANTHROPIC_API_KEY=sk-ant-YOUR-API-KEY-HERE

# Optional: Port to run the server on (default: 8080)
export PORT=8080
